// LUX Runtime, Book III Part E: the Guardian Gate predicate.
// The functions live in ../engine.mjs, the file that holds all protocol logic.
// A Genesis calibration result is reported, never sealable.
export { LUXViolation, calculateMetrics, verify } from '../engine.mjs';
